fastapi==0.111.0
uvicorn[standard]==0.29.0
gunicorn==22.0.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
python-multipart==0.0.9

# Google Earth Engine (optional — runs in mock mode if not installed)
earthengine-api==0.1.409
# geemap is heavy; only needed for GeoTIFF export from GEE
# Install manually if deploying with real GEE credentials:
#   pip install geemap==0.31.0

# Raster / image
# rasterio requires GDAL system libraries — install only when needed:
#   pip install rasterio==1.3.10
Pillow==10.3.0
numpy==1.26.4
shapely==2.0.4

# HTTP
httpx==0.27.0
requests==2.32.2

# LLM Fallback / Dynamic Analysis
openai>=1.0.0

# Export
reportlab==4.2.0

# Utilities
python-jose==3.3.0
aiofiles==23.2.1
